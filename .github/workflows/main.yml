name: Build Mac

on:
  workflow_dispatch: {}
    
permissions:
  contents: write

jobs:
  build-windows:
    runs-on: ubuntu-latest
    steps:
    - run: clang --version
    - run: clang-18 --version
